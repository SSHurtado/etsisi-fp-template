\documentclass{fp}

% The class already loads the packages used below for figures, tables,
% wrapped figures, landscape pages, captions, colors, hyperlinks, and
% bibliography. Add project-specific packages here only when needed.

\begin{document}

% ---------------------------------------------------------------------------
% Full-page cover image.
% Replace the PNG with your own A4 cover, keeping the same path if possible.
% ---------------------------------------------------------------------------
\coverpage{images/covers/cover.png}

% ---------------------------------------------------------------------------
% License page.
% Available license keys are the PNG filenames in images/licenses without .png:
% CC-0, CC-BY, CC-BYND, CC-BYNC, CC-BYNCND, CC-BYNCSA, CC-BYSA.
% ---------------------------------------------------------------------------
\begin{licensepage}{CC-BYSA}
  {YOUR PROJECT TITLE\\[0.5ex]
   A short subtitle that explains the scope of the work.}
  {Your Name}
  {Tutor Name}
  {\href{https://github.com/SSHurtado/etsisi-fp-template}{%
    \includegraphics[width=0.58\linewidth]{images/others/codeQR.png}%
  }}
  {This template also demonstrates inline bibliographic entries such as
   \fullcite{templatepaper}.}
  {Madrid, Month 2026}
\end{licensepage}

\acknowledgmentpage{Use this page for short acknowledgements. The command
accepts one argument, so write the complete text between braces. You can still
use emphasis, links, citations, and the styled inline code command such as
\texttt{acknowledgmentpage}.}

\abstractpage{This abstract page is generated with \texttt{\string\abstractpage}.
The first argument is the abstract text. Use it to summarize the research
problem, the methodology, the main results, and the practical relevance of your
work in one or two compact paragraphs. The class uses custom fonts, spacing,
and page layout for this front-matter page.}
{LaTeX template, final degree project, reproducible writing}

\toc
\lof
\lot

\section{Template Overview}
\label{sec:overview}

This document is intentionally small, but it exercises the visual and structural
features provided by \texttt{fp.cls}. Use it as a starting point by replacing
the sample text, figures, bibliography entries, and metadata with your own
project material.

The class configures the page geometry, headers, footers, fonts, section titles,
caption style, hyperlink colors, bibliography style, and several front-matter
commands. It also redefines \texttt{\string\texttt} so inline code has the same
visual treatment throughout the document.

\subsection{Headings, Links, and Code}
\label{subsec:headings}

Sections automatically begin on a new page and are rendered with the class
colors. Subsections receive a rule under the title, while subsubsections use an
italic style.

\subsubsection{A Third-Level Heading}
\label{subsubsec:third-level}

Internal links, citations, and web links use different colors. For example, see
\autoref{fig:single}, the table examples in \autoref{sec:tables}, and the
sample source in \cite{templatedataset}. A web link can be written with
\href{https://www.overleaf.com/}{Overleaf}.

\begin{tcolorbox}[
  colback=rowshade,
  colframe=sectionblue,
  coltitle=white,
  title={Class color palette},
  sharp corners,
  boxrule=0.8pt
]
The class defines reusable colors such as \texttt{sectionblue},
\texttt{subsectiongray}, \texttt{rowshade}, \texttt{reflinkcolor},
\texttt{weblinkcolor}, and \texttt{intlinkcolor}. It also loads
\texttt{tcolorbox}, so you can build callouts with the same palette.
\end{tcolorbox}

\section{Figures and Visualizations}
\label{sec:figures}

The class loads \texttt{graphicx}, \texttt{caption}, \texttt{subcaption},
\texttt{wrapfig}, and \texttt{pdflscape}. The examples below show the most
common figure patterns for a final project document.

\subsection{Single Figure}
\label{subsec:single-figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.82\linewidth]{images/plots/demo_chart.png}
  \captionsetup{width=0.82\linewidth}
  \caption[Short figure title for the list of figures]{A standard figure using
  the class caption format. The optional caption text appears in the list of
  figures, while this longer caption appears below the image.}
  \label{fig:single}
\end{figure}

\subsection{Subfigures}
\label{subsec:subfigures}

\begin{figure}[htbp]
  \centering
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{images/plots/demo_plot_a.png}
    \caption{First visualization.}
    \label{fig:sub-a}
  \end{subfigure}
  \hfill
  \begin{subfigure}[t]{0.48\linewidth}
    \centering
    \includegraphics[width=\linewidth]{images/plots/demo_plot_b.png}
    \caption{Second visualization.}
    \label{fig:sub-b}
  \end{subfigure}
  \captionsetup{width=\linewidth}
  \caption[Subfigure layout]{A two-panel figure built with the
  \texttt{subcaption} package loaded by the class. You can reference panels as
  \autoref{fig:sub-a} and \autoref{fig:sub-b}.}
  \label{fig:subfigures}
\end{figure}

\subsection{Wrapped Figure}
\label{subsec:wrapped}

\begin{wrapfigure}[13]{r}{0.36\linewidth}
  \centering
  \includegraphics[width=\linewidth]{images/plots/demo_wrap.png}
  \captionsetup{width=\linewidth}
  \caption[Wrapped figure]{A wrapped figure beside paragraph text.}
  \label{fig:wrapped}
\end{wrapfigure}

Wrapped figures are useful when a compact visual supports the surrounding
discussion without needing a full-width float. Keep the wrapped figure close to
the paragraph that introduces it, and avoid placing it near section boundaries
where float placement becomes less predictable.

This paragraph adds enough text to show the wrapping behavior. In a thesis,
wrapped figures work best for diagrams, icons, or small plots that remain
legible at a reduced width. Larger visualizations should use a normal figure or
a landscape page instead.

\section{Tables}
\label{sec:tables}

The \texttt{mytable} environment is defined in \texttt{fp.cls}. Its arguments
are placement, column specification, long caption, label, and an optional short
caption for the list of tables. In practice, that means the table below is read
as a table with placement, column layout, caption, label, and optional short
caption arguments.
Use the class environment for most thesis tables because it already applies the
caption width, row shading, and list-of-tables entry.

\subsection{Default Class Table}
\label{subsec:default-table}

\setlength{\tabcolsep}{3pt}
\renewcommand{\cellfont}{\footnotesize}
\begin{mytable}[htbp]
  {p{0.34\linewidth} p{0.34\linewidth} p{0.20\linewidth}}
  {Main custom commands and environments exposed by \texttt{fp.cls}. This is
  the recommended style for regular descriptive tables.}
  {tab:class-features}
  [Class features]
  \thead{Feature} & \thead{Purpose and arguments} & \thead{Typical use} \\
  \hline
  \texttt{\string\coverpage} & Creates a full-page image cover. &
  First page of the document. \\
  \texttt{licensepage} & Builds the metadata and Creative Commons page. &
  Title, author, tutor, QR, notes, and date. \\
  \texttt{\string\acknowledgmentpage} & Adds a styled acknowledgements page. &
  Optional front matter before the abstract. \\
  \texttt{\string\abstractpage} & Adds abstract text and keywords. &
  Summary and indexing terms. \\
  \texttt{\string\toc}, \texttt{\string\lof}, \texttt{\string\lot} &
  Prints styled contents, figures, and tables lists. &
  Front matter after the abstract. \\
  \texttt{mytable} & Applies row shading and caption formatting. &
  Reusable data tables. \\
\end{mytable}
\renewcommand{\cellfont}{\small}
\setlength{\tabcolsep}{6pt}

\subsection{Compact Numeric Table}
\label{subsec:numeric-table}

For metrics, ablation studies, or experiment summaries, a compact table with a
colored header is usually easier to scan than wide text columns. The example in
\autoref{tab:numeric-results} uses a normal \texttt{table} and \texttt{tabular}
environment, while still using the class colors.

\begin{table}[htbp]
  \centering
  \small
  \captionsetup{width=0.85\linewidth}
  \renewcommand{\arraystretch}{1.18}
  \rowcolors{2}{rowshade}{white}
  \begin{tabular}{l c c c c}
    \rowcolor{sectionblue}
    \textcolor{white}{\librefranklinbold Model} &
    \textcolor{white}{\librefranklinbold MAE} &
    \textcolor{white}{\librefranklinbold RMSE} &
    \textcolor{white}{\librefranklinbold Time} &
    \textcolor{white}{\librefranklinbold Rank} \\
    Persistence & 0.184 & 0.271 & 0.1 s & 4 \\
    Linear baseline & 0.132 & 0.206 & 0.4 s & 3 \\
    Transformer small & 0.091 & 0.148 & 1.8 s & 2 \\
    Transformer ensemble & \textbf{0.074} & \textbf{0.119} & 4.7 s & 1 \\
  \end{tabular}
  \rowcolors{2}{}{}
  \renewcommand{\arraystretch}{1}
  \caption[Compact numeric table]{Compact numeric table with a colored header,
  alternating rows, and aligned metric columns.}
  \label{tab:numeric-results}
\end{table}

\subsection{Decision Matrix}
\label{subsec:decision-matrix}

The decision matrix style in \autoref{tab:decision-matrix} is useful when you
need to compare methods, datasets, risks, or tools across qualitative criteria.
The first column is highlighted, and the criteria columns use concise labels so
the table stays readable.

\begin{table}[htbp]
  \centering
  \footnotesize
  \captionsetup{width=\linewidth}
  \renewcommand{\arraystretch}{1.25}
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{
    >{\raggedright\arraybackslash}p{0.21\linewidth}
    >{\centering\arraybackslash}p{0.13\linewidth}
    >{\centering\arraybackslash}p{0.15\linewidth}
    >{\centering\arraybackslash}p{0.17\linewidth}
    >{\raggedright\arraybackslash}p{0.20\linewidth}
  }
    \rowcolor{headergray}
    \thead{Option} & \thead{Cost} & \thead{Accuracy} & \thead{Explainability}
    & \thead{Recommendation} \\
    \hline
    \cellcolor{rowshade}\librefranklinbold Baseline model &
    Low & Medium & High & Keep as a sanity check. \\
    \cellcolor{rowshade}\librefranklinbold Classical ML &
    Medium & Medium & Medium & Useful for fast iteration. \\
    \cellcolor{rowshade}\librefranklinbold Deep model &
    High & High & Low & Use when performance justifies complexity. \\
    \cellcolor{rowshade}\librefranklinbold Hybrid approach &
    High & High & Medium & Strong candidate for final experiments. \\
  \end{tabular}
  \renewcommand{\arraystretch}{1}
  \setlength{\tabcolsep}{6pt}
  \caption[Decision matrix]{Decision matrix with a shaded option column and
  compact qualitative criteria.}
  \label{tab:decision-matrix}
\end{table}

\subsection{Milestone Table}
\label{subsec:milestone-table}

Use a milestone table when the document needs to explain the execution plan or
show project progress. The grouped header in \autoref{tab:milestones} is made
with \texttt{multicolumn}, which is available in standard LaTeX.

\begin{table}[htbp]
  \centering
  \small
  \captionsetup{width=0.92\linewidth}
  \renewcommand{\arraystretch}{1.2}
  \begin{tabular}{p{0.22\linewidth} p{0.18\linewidth} p{0.18\linewidth} p{0.28\linewidth}}
    \rowcolor{sectionblue}
    \multicolumn{4}{c}{\textcolor{white}{\librefranklinbold Example project schedule}} \\
    \rowcolor{headergray}
    \thead{Milestone} & \thead{Status} & \thead{Evidence} & \thead{Next action} \\
    \hline
    Literature review & \textcolor{sectionblue}{Done} & Annotated bibliography &
    Move key references into \texttt{references.bib}. \\
    Dataset preparation & \textcolor{reflinkcolor}{In progress} & Cleaning script &
    Freeze the preprocessing version used in results. \\
    Model evaluation & Pending & Experiment plan & Select final metrics and
    comparison baselines. \\
    Final manuscript & Pending & Template draft & Replace placeholder figures
    and tables with project material. \\
  \end{tabular}
  \renewcommand{\arraystretch}{1}
  \caption[Milestone table]{Milestone table with a grouped title row, status
  colors, and action-oriented notes.}
  \label{tab:milestones}
\end{table}

\section{Wide Material}
\label{sec:wide}

Use the \texttt{landscape} environment when a wide diagram or table would be too
cramped on a portrait page. Wide tables should be reserved for material that
genuinely benefits from side-by-side comparison; otherwise, split the table into
smaller thematic tables.

\begin{landscape}
\begin{figure}[p]
  \centering
  \includegraphics[width=0.92\linewidth]{images/plots/landscape_timeline.png}
  \captionsetup{width=0.9\linewidth}
  \caption[Landscape visualization]{A wide figure placed on a landscape page
  with the \texttt{pdflscape} package loaded by the class.}
  \label{fig:landscape}
\end{figure}
\end{landscape}

\begin{landscape}
\begin{table}[p]
  \centering
  \small
  \captionsetup{width=0.88\linewidth}
  \renewcommand{\arraystretch}{1.2}
  \rowcolors{2}{rowshade}{white}
  \begin{tabular}{
    p{0.18\linewidth}
    p{0.18\linewidth}
    p{0.18\linewidth}
    p{0.18\linewidth}
    p{0.18\linewidth}
  }
    \rowcolor{sectionblue}
    \textcolor{white}{\librefranklinbold Component} &
    \textcolor{white}{\librefranklinbold Inputs} &
    \textcolor{white}{\librefranklinbold Method} &
    \textcolor{white}{\librefranklinbold Output} &
    \textcolor{white}{\librefranklinbold Validation} \\
    Data ingestion & Raw observations, metadata, timestamps &
    Parsing, filtering, unit checks & Versioned analysis table &
    Missing-value report and schema checks \\
    Feature design & Clean time series and domain indicators &
    Rolling windows, normalization, lag features & Training matrix &
    Distribution plots and leakage checks \\
    Model training & Training and validation splits &
    Baseline comparison and selected architecture & Candidate models &
    Metric table and residual analysis \\
    Reporting & Final metrics, figures, and references &
    Final manuscript and reproducible build & Submitted report &
    PDF review and bibliography audit \\
  \end{tabular}
  \rowcolors{2}{}{}
  \renewcommand{\arraystretch}{1}
  \caption[Landscape table]{Landscape table for workflow, architecture, or
  experiment-pipeline summaries that need more horizontal space.}
  \label{tab:landscape-table}
\end{table}
\end{landscape}

\section{Bibliography}
\label{sec:bibliography}

The class loads \texttt{biblatex} and expects a file named
\texttt{references.bib}. Cite sources normally with \texttt{\string\cite}, for
example \cite{templatepaper}, and print the bibliography with the custom
\texttt{mybigheading} heading:

\printbibliography[heading=mybigheading,title={References}]

\end{document}
